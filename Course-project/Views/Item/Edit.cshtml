@model Course_project.Models.Item
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
<h2>@Localizer["NewItem"]</h2>
<form method="post" asp-controller="Item" asp-action="Edit" asp-route-ItemId="@ViewBag.Item.Id" enctype="multipart/form-data">
    <div class="col-md-4">
        <div class="form-group">
            <label asp-for="Name">@Localizer["Name"]</label><br />
            <input asp-for="Name" class="form-control" value="@ViewBag.Item.Name" />
        </div>
        <div class="form-group">
            <label asp-for="Description">@Localizer["Description"]</label><br />
            <textarea class="form-control" rows="3" asp-for="Description" value="@ViewBag.Item.Description">@ViewBag.Item.Description</textarea>
        </div>
        @*<dl class="row">
                <dt class="col-sm-">
                    <div class="form-group">
                        <label asp-for="Img" class="control-label">@Localizer["Img"]</label>
                        <input type="file" asp-for="Img" class="form-control" value="@ViewBag.Collection.UrlImg" />
                        <span asp-validation-for="Img" class="text-danger"></span>
                    </div>
                </dt>
                <dd class="col-sm-10">
                    <img src="@ViewBag.Collection.UrlImg" alt="Image" style="width: 150px; height: 220px;" />
                </dd>
            </dl>*@
        <div>
            <button type="submit" class="btn btn-primary mt-1">@Localizer["Create"]</button>
        </div>
    </div>
</form>