@{
    ViewBag.Title = "List of Users";
}
<h2>List of Users</h2>
<form id='form1' method="post">
    <button type="submit" class="btn btn-primary m-1" asp-controller="Admin" asp-action="Delete">Delete</button>
    <button type="submit" class="btn btn-primary m-1" asp-controller="Admin" asp-action="Block">Block</button>
    <button type="submit" class="btn btn-primary m-1" asp-controller="Admin" asp-action="UnBlock">UnBlock</button>

    <table class="table table-striped table-hover">
        <tr>
            <th>Email</th>
            <th>Name</th>
            <th>Roles</th>
            <th>Status</th>
            <th><input type="checkbox" name="cb_all"></th>
        </tr>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.Email</td>
                <td><a asp-controller="Profile" asp-action="Index" asp-route-userid="@user.Id">@user.UserName</a></td>
                <td><a class="btn btn-sm btn-primary text-center" asp-controller="Roles"  asp-action="Edit" asp-route-userid="@user.Id">Roles</a></td>
                <td>@user.Status</td>
                <td><input type="checkbox" class="qwe" name="selectedUsers" value="@user.UserName" /></td>
            </tr>
        }
    </table>
</form>
<a class="btn btn-sm btn-primary" asp-controller="Roles" asp-action="Create">Add Role</a>
<a class="btn btn-sm btn-primary" asp-controller="Roles" asp-action="Index">Roles List</a>

<script>
    var f = document.getElementById('form1');
    f.cb_all.onchange = function (e) {
        var el = e.target || e.srcElement;
        var qwe = el.form.getElementsByClassName('qwe');
        for (var i = 0; i < qwe.length; i++) {
            if (el.checked) {
                qwe[i].checked = true;
            } else {
                qwe[i].checked = false;
            }
        }
    }
</script>
